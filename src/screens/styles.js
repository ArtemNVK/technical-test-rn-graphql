import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        top: 10,
        alignItems: "center",
    },
    title: {
        color: " #7c7c7c",
        fontWeight: '700',
        opacity: 0.7
    }
});

export default styles;